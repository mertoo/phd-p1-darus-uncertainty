data:
  base_path: "data/processed/darus"
  history: 30
  horizon: 30
  batch_size: 256

model:
  type: "mlp"
  hidden_dim: 256
  num_layers: 3      
  dropout: 0.0

training:
  epochs: 20
  lr: 0.001
  device: "cuda"

logging:
  save_dir: "experiments/results"
  run_name: "p1_mlp_baseline"

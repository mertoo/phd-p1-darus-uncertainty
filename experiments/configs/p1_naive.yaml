data:
  base_path: "data/processed/darus"
  history: 30
  horizon: 30
  batch_size: 256

model:
  type: "naive"

training:
  epochs: 1        # ignored, just keeping for consistency
  lr: 0.001
  device: "cuda"

logging:
  save_dir: "experiments/results"
  run_name: "p1_naive_baseline"
